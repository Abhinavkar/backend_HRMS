from django.urls import path
from . import project_management_app_views as views  # Importing views

urlpatterns = [
    # Define your project management URLs here
    # path('list/', views.ProjectListView.as_view(), name='project_list'),
    # path('detail/<int:id>/', views.ProjectDetailView.as_view(), name='project_detail'),
    # path('create/', views.ProjectCreateView.as_view(), name='project_create'),
    # path('update/<int:id>/', views.ProjectUpdateView.as_view(), name='project_update'),
    # path('delete/<int:id>/', views.ProjectDeleteView.as_view(), name='project_delete'),
]
